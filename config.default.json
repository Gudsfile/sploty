{
    "READ_ME": "This file contains configuration variables that shouldn’t be in version control. This includes things like API keys and database URIs containing passwords. This also contains variables that are specific to this particular instance of your application. For example, you might have DEBUG = False in config.py, but set DEBUG = True in instance/config.py on your local machine for development. Since this file will be read in after config.py, it will override it and set DEBUG = True.",
    "logger": {
        "debug": true
    },
    "file": {
        "resources_folder": "resources/MyData",
        "chunk_size": 50
    },
    "spotify": {
        "timeout": 10,
        "s_sleep": 60,
        "client_id": "TO BE RENSEIGNED",
        "client_secret": "TO BE RENSEIGNED",
        "auth_url": "https://accounts.spotify.com/api/token",
        "base_url": "https://api.spotify.com/v1/"
    },
    "elasticsearch": {
        "hosts": [
            "http://localhost:9200"
        ],
        "username": "elastic",
        "password": "changeme",
        "request_timeout": 10,
        "index": {
            "name": "spotify-stream-history-v1"
        }
    },
    "tinydb": {
        "track_audio_features": "resources/db/spotify/track_audio_features.json"
    }
}
